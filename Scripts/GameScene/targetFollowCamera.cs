using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class targetFollowCamera : MonoBehaviour
{
    //[SerializeField]
    //private GameObject cam;
    //[SerializeField]
    //private GameObject target;

    //float rotateSpeed = 10;
    //float xRotation = 0f;
    void Update()
    {
        //Vector3 pos = cam.GetComponent<Camera>().WorldToViewportPoint(transform.position);
        //pos.x = Mathf.Clamp01(pos.x);
        //pos.y = Mathf.Clamp01(pos.y);
        //target.transform.position = cam.GetComponent<Camera>().ViewportToWorldPoint(pos);
        //target.transform.position = cam.transform.forward;

        

        //target.transform.position += new Vector3(0, cam.transform.rotation.x, 0);
    }
}
